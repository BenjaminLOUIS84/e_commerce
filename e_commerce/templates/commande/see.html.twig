{# {% extends 'base.html.twig' %} #}

{% block title %}{% endblock %}

{% block body %}
    <!DOCTYPE html>
    <html lang="fr">

        <head>
            <meta charset="utf-8">

            <link rel="stylesheet" href="{{ asset('css/file.css') }}">
            
            <meta http-equiv="X-UA-Compatible" content="IE=edge">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville&display=swap" rel="stylesheet">


            <title>ADMIN Détail de la commande</title>
        </head>
        {# Détail de la commande cible à savoir la liste des livres #}
        <h3>Détail de la commande du {{ commande.dateCommande | date("d-m-Y") }}</h3>
        <body>
            <div class="wrapperLivres">

                {# Afficher les coordonnées du client #}
                <div class="order">
                    {{ commande.coordonnees }}

                    <div class="articles">
                        
                        {# Créer une condition pour afficher un message quand il y a pas de livres dans une commande #}
                        
                        {% if commande.commandeLivres | length >0 %}
                            
                            {% for commandeLivre in commande.commandeLivres %}
                                
                                <figure>
                                    {# Pour afficher les couvertures de chaque livres #}
                                    <img src="{{ asset('uploads/images/'~ livre.couverture) }}" alt="Couverture des Livres de la collection sélectionnée" class="cover">
                                </figure>
                        
                            {% endfor %}

                        {% else %}

                            <h2>Aucuns livres dans cette commande</h2>
                    
                        {% endif %} 

                    </div>
                
                    <nav id="men">
                        <div id="menu">
                            <a href="{{ path ('app_commande') }}"><img class="lk" src="{{ asset('img/backward-step-solid.svg') }}" alt="Retour à la liste des commandes" title="Retourner à la liste des commandes"></a>
                        </div>
                    </nav>
                </div>    
            </div>
        </body>

    </html>

{% endblock %}
